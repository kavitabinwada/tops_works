#include<stdio.h>
#include<string.h>
int main(){
    char str[40];
    printf("\n enter a string to be reversed:");
    scanf("%c",str);
    printf("\n after the reverse of a string:%s",strlen(str));
}